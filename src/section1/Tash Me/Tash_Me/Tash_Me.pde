PImage mustache;
PImage friend;
void setup() {
friend = loadImage("friend.png");
mustache = loadImage("mustache.png");
size(800,800);
friend.resize(800,800);
mustache.resize(355,290);
}

void draw() {
background(friend);
if (mousePressed); {
  
}
  
  
image(mustache, 237, 365);
}
